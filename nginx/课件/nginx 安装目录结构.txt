（1）conf 目录 —— 下面最重要的时 nginx.conf，这个文件中引用了其他的配置文件
（2）html 目录 —— 该目录主要时存储静态页面资源
（3）sbin 目录 —— 下面的 nginx 是运行 nginx 的代码
（4）logs 目录 —— 下面主要存储关于 nginx 的日志文件，包含了用户访问的 access.log 和系统出错时的 error.log 文件以及 nginx主程序的 pid 文件；