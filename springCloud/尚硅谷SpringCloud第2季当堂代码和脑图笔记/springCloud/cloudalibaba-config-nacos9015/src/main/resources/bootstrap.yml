# nacos配置
server:
  port: 9015

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 112.74.97.91:8848 #Nacos服务注册中心地址
      config:
        server-addr: 112.74.97.91:8848 #Nacos作为配置中心地址
        file-extension: yml #指定yaml格式的配置
        prefix: nacos
        namespace: 8490cae9-b702-4c4b-8949-77e9675f6241


# ${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension}
# nacos-dev.yml
